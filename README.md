Metodos Implementados:
-> Algoritmo A Star para evaluar la ruta mas corta, en base a pos I, Pos F y el costo de cada casilla con f,g,h (Metodo Manhattan)

Optimizacion:
-> Se utilizo Hashes para poder crear y eliminar set() de manera rapida sin saturar el cpu, debido a que al hacer el
  calculo de las posibilidades era necesario.
